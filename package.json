{
  "name": "crud-product",
  "version": "1.0.0",
  "scripts": {
    "server": "json-server --watch server/db.json --delay 2000 --port 4000"
  },
  "devDependencies": {
    "json-serve": "^13.0.2"
  }
}
